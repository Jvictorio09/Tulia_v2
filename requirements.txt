# Core Django and dependencies
Django==4.2.26
asgiref==3.9.1
sqlparse==0.5.1
pytz==2025.2
tzdata==2025.2

# Database
dj-database-url==2.3.0
psycopg[binary]>=3.2.4

# Environment & Configuration
python-dotenv==1.0.1
django-environ==0.10.0

# HTTP & API clients
requests==2.32.3
certifi==2024.8.30
charset-normalizer==3.4.0
idna==3.10
urllib3==2.2.3

# AI/ML
openai==1.51.0
# OpenAI dependencies
pydantic==2.9.2
pydantic_core==2.23.4
annotated-types==0.7.0
typing_extensions==4.12.2
sniffio==1.3.1
anyio==4.5.2
httpx==0.27.2
httpcore==1.0.6
h11==0.14.0

# Production Server
gunicorn==21.2.0

# Static Files (for production)
whitenoise==6.7.0

# Utilities
python-dateutil==2.8.2
six==1.17.0

# ============================================
# REMOVED PACKAGES (not found in codebase):
# ============================================
# - celery, channels, redis, daphne, autobahn, Twisted (WebSocket/async not configured)
# - cloudinary, django-cloudinary-storage (only URLs in seed data, no library usage)
# - stripe, squareup (payment processing not implemented)
# - faiss-cpu, pandas, numpy (ML/data processing not used)
# - gTTS, deep-translator, pytesseract, PyMuPDF, reportlab, qrcode (not used)
# - msal (Microsoft authentication not used)
# - django-debug-toolbar (not in INSTALLED_APPS)
# - django-countries (not in INSTALLED_APPS)
# - django-widget-tweaks (not in INSTALLED_APPS)
# - djangorestframework (not in INSTALLED_APPS)
# - APScheduler (not used)
# - beautifulsoup4 (not used)
# - openpyxl (not used)
# - python-docx (not used)
# - langdetect (not used)
# - git-filter-repo (development tool, not runtime dependency)
